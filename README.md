# thiagoprocaci.github.io

Personal site, built with [Hugo](https://gohugo.io). Source is in `hugo_source/`; everything else at the root is generated output served by GitHub Pages.

    ./build.sh   # regenerate the site into the repository root
