# thiagoprocaci.github.io (Hugo source)

Content and configuration live here. The theme is `erblog` (vendored under `themes/`).

- Preview: `hugo server -D` and open http://localhost:1313
- Publish: run `../build.sh` from the repo root. It builds into `public/` and copies the output to the repository root, which GitHub Pages serves.
